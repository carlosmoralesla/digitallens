<%= form_for(lent_kind) do |f| %>
  <% if lent_kind.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lent_kind.errors.count, "error") %> prohibited this lent_kind from being saved:</h2>

      <ul>
      <% lent_kind.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
